# グルメ即決アプリ（仮）設計書 v1

> 4タップで「今日の一軒」を言い切るWebアプリ。
> ターゲット：大学生。主戦場：デートと飲み会（幹事）。入口：Instagram。

---

## 1. プロダクト原則（全設計判断はここに従う）

1. **キーボードを一度も出さない。** 全入力はタップのみ。
2. **4問で終わる。** Q1（シーン）＋ Q2〜Q4（シーン別）。30秒以内。
3. **候補を並べず、1店を言い切る。** 一覧を出したら既存グルメサイトと同じ。代案は「違うのがいい」で1件ずつ。
4. **決定の責任をアプリが引き受ける。** 理由タグで「なぜこの店か」を必ず添える。
5. **URLがすべて。** インストール不要・結果はLINEで送れる・共有された側もそのまま使える。

## 2. 全体フロー

```
[着地] → (位置許可 or エリアタップ) → Q1 誰と？ → Q2 → Q3 → Q4
      → [結果: 1店ドン]
           ├─ ここにする → ホットペッパー店舗ページ / 地図
           ├─ 違うのがいい → 次点（最大5件、その後「条件を変える」）
           └─ LINEで送る → 結果URL共有
```

### Q0（質問にしない・自動で決めるもの）
| 項目 | 決め方 |
|---|---|
| 位置 | ホームの「📍」チップでエリア選択（現在地＋首都圏12エリア、localStorage に記憶）。「現在地」選択時は Geolocation API、拒否時は渋谷にフォールバック |
| 検索半径 | 既定 `range=3`（1000m）。シーンD（ひとり）のみQ4で距離を聞く |
| 時間帯 | 端末時計。**10:00–14:59 はランチ文脈**：予算選択肢をランチ表に差し替え、シーンA/Dは `lunch=1` を付与 |

## 3. 質問ツリー（4シーン × 3問 × 4択）

エンジンの役割分担（重要）：
- **ハード条件（APIパラメータで絞る）**: エリア・予算・ジャンル・人数・マスト設備
- **ソフト条件（取得後にクライアントでスコアリング）**: 雰囲気・テンション系。catch文のキーワード一致と設備フラグで加点

こうする理由：ホットペッパーAPIに「雰囲気」フィールドは無いため、雰囲気をハード条件にすると0件が頻発する。雰囲気は必ずソフト側に置く。

---

### Q1. 「今日はだれと？」

| 選択肢 | → シーン |
|---|---|
| 🌹 ふたりで | A. デート |
| 🍻 友達と | B. 飲み・ごはん |
| 🎓 幹事です | C. サークル・ゼミ・バイト先の会 |
| 🍜 ひとりで | D. サク飯 |

---

### シーンA：デート 🌹

**Q2. 「どんな空気にしたい？」（→ソフト条件）**

| 選択肢 | スコアリングキーワード | 加点フラグ |
|---|---|---|
| 🕯 しっぽり落ち着いて | 隠れ家/落ち着/静か/大人/カウンター | private_room |
| ✨ おしゃれに | おしゃれ/デート/映え/ワイン/テラス | open_air |
| 😆 気取らずわいわい | 大衆/気軽/賑/名物/串 | — |
| 🌃 特別な日っぽく | 夜景/記念日/コース/個室/雰囲気 | night_view, course |

**Q3. 「ひとり予算は？」（→ハード条件 budget）**

| 選択肢（夜） | budgetコード | ランチ時の表示・コード |
|---|---|---|
| 〜2,000円 | B011,B001 | 〜1,000円 → B009,B010 |
| 2,000〜4,000円 | B002,B003 | 1,000〜1,500円 → B011 |
| 4,000〜7,000円 | B008,B004 | 1,500〜2,000円 → B001 |
| 7,000円〜 | B005,B006 | 2,000円〜 → B002,B003 |

**Q4. 「おなかの気分は？」（→ハード条件 genre）**

| 選択肢 | genreコード |
|---|---|
| 🥩 がっつり肉 | G008(焼肉),G017(韓国),G007(中華) |
| 🐟 和でさっぱり | G004(和食),G003(創作) |
| 🍷 お酒と小皿でゆっくり | G012(バー),G002(ダイニングバー・バル),G001(居酒屋) |
| 🍰 甘いもの・カフェ | G014(カフェ・スイーツ) |

---

### シーンB：友達と飲み・ごはん 🍻

**Q2. 「何人くらい？」（→ハード条件）**

| 選択肢 | パラメータ |
|---|---|
| 2〜3人 | 指定なし |
| 4〜6人 | party_capacity=6 |
| 7〜10人 | party_capacity=10 |
| 11人〜 | party_capacity=15 |

**Q3. 「ひとりいくらまで？」（→ハード条件 budget）**

| 選択肢 | budgetコード |
|---|---|
| 〜2,000円 | B011,B001 |
| 〜3,000円 | B002 |
| 〜4,000円 | B003 |
| 4,000円〜 | B008,B004 |

**Q4. 「今日のテンションは？」（→ハード＋ソフト混合）**

| 選択肢 | ハード | ソフト（キーワード） |
|---|---|---|
| 🏮 大衆酒場でわいわい | genre=G001 | 大衆/昭和/レトロ/せんべろ/ハイボール |
| 🚪 個室でまったり | private_room=1, genre=G001,G004 | 個室/ゆったり |
| 🍸 おしゃれにバル | genre=G002,G012 | バル/ワイン/クラフト/肉バル |
| 🌙 終電気にせず朝まで | midnight=1, genre=G001,G012,G011 | 深夜/朝まで/2軒目 |

---

### シーンC：幹事モード（サークル・ゼミ・バイト先）🎓

> 幹事の絶対条件：**全員入れる・予算内・マスト設備**。ここだけはハード条件を厚くし、フォールバックでも緩めない。

**Q2. 「何人の会？」（→ハード条件）**

| 選択肢 | パラメータ |
|---|---|
| 4〜6人 | party_capacity=6 |
| 7〜10人 | party_capacity=10 |
| 11〜20人 | party_capacity=20 |
| 21人〜 | party_capacity=25（＋ソフト: 貸切/charter加点） |

**Q3. 「マスト条件は？」（→ハード条件・絶対に緩めない）**

| 選択肢 | パラメータ |
|---|---|
| 🍺 飲み放題は必須 | free_drink=1 |
| 🚪 個室は必須 | private_room=1 |
| 💪 両方ほしい | free_drink=1, private_room=1 |
| 🤷 こだわらない | 指定なし（course=1をソフト加点） |

**Q4. 「ひとり予算は？」（→ハード条件 budget、course をソフト加点）**

| 選択肢 | budgetコード |
|---|---|
| 〜2,500円（節約会） | B001,B002 |
| 〜3,500円（いつもの） | B002,B003 |
| 〜4,500円（ちゃんとした会） | B003,B008 |
| 4,500円〜（OB会・追いコン） | B008,B004 |

共通ソフトキーワード：宴会/飲み放題/コース/貸切/歓迎会/送別会

---

### シーンD：ひとりでサク飯 🍜

**Q2. 「おなかの気分は？」（→ハード条件 genre）**

| 選択肢 | genreコード | ソフト |
|---|---|---|
| 🍖 がっつり | G013,G008,G017 | デカ盛り/定食/スタミナ |
| 🥗 軽く・ヘルシー | G004,G003,G014 | 定食/野菜/ヘルシー |
| 🍜 麺 | G013 | つけ麺/うどん/そば（※うどん/そばはG004も追加取得して麺キーワードで抽出） |
| ☕ カフェで一息 | G014 | 電源/wifi（wifi=1をソフト加点） |

**Q3. 「予算は？」（→ハード条件）**

| 選択肢 | budgetコード |
|---|---|
| 〜1,000円 | B009,B010 |
| 〜1,500円 | B011 |
| 〜2,000円 | B001 |
| 気にしない | 指定なし |

**Q4. 「どこまで歩ける？」（→ハード条件 range）**

| 選択肢 | パラメータ |
|---|---|
| 🏃 すぐそこ（300m） | range=1 |
| 🚶 5分（500m） | range=2 |
| 🚶 10分ちょい（1km） | range=3 |
| 🚌 足を伸ばす（3km） | range=5 |

---

## 4. 決定エンジン仕様

```
1. ハード条件でAPI検索
   GET /hotpepper/gourmet/v1/?key=..&lat=..&lng=..&range=3
       &genre=..&budget=..&(party_capacity/free_drink/private_room/midnight)
       &order=4&count=100&format=json
2. クライアントでスコアリング
   score = catch/genre.catch/name へのソフトキーワード一致 ×2
         + 加点フラグ一致（night_view等） ×3
         + おススメ順の逆順位 ×0.1
3. 同点帯はシャッフル。シード = 日付＋回答ハッシュ
   （同じ日に同じ回答なら同じ店＝共有時に結果がブレない。日が変われば変わる）
4. 上位5件を「提案キュー」に。1位を言い切り表示、
   「違うのがいい」で2位以降を1件ずつ。5件使い切ったら「条件を変える」導線。
```

### 0件・少数件フォールバック（この順で緩める）
1. budget を隣接コードへ1段階拡張
2. range を1段階拡張（3→4→5）
3. Q2/Q4由来の**ソフト以外の**雰囲気系ハードフラグを外す（midnight は意図が強いので維持）
4. genre を隣接ジャンルへ拡張
- **幹事モードの party_capacity / free_drink / private_room は最後まで絶対に緩めない**
- 緩めた場合は結果画面に「予算すこし超えるかも」等の正直タグを必ず表示
- それでも0件 → 「このへんだと見つからず。エリアを広げる？」

## 5. 結果画面仕様

| 要素 | 内容（APIフィールド） |
|---|---|
| 写真 | photo.pc.l |
| 店名・キャッチ | name / catch / genre.catch |
| 予算 | budget.average |
| アクセス | mobile_access |
| **理由タグ（2〜3個・必須）** | 回答と一致した根拠：「個室あり」「飲み放題」「夜景◎」「予算内」「駅チカ」 |
| CTA1: ここにする | urls.pc（ホットペッパー店舗ページ＝クーポン/予約）＋ Googleマップリンク(lat,lng) |
| CTA2: 違うのがいい | 提案キュー次点へ |
| CTA3: LINEで送る | 結果URL（shop id＋回答をクエリに符号化）→ 受け手も同じ結果画面に着地 |
| クレジット | **「Powered by ホットペッパーグルメ Webサービス」表記必須（利用規約）** |

## 5.5. データ経路（v2で変更）

ブラウザからホットペッパーAPIを直接呼ぶ方式は、CORS非対応・JSONPの遮断により実機で失敗した（直接／公開中継4経路すべて不通を確認）。そこで **サーバー側での事前取得** に変更した。

```
GitHub Actions（毎日4時JST）
  → ホットペッパーAPI（エリア11 × ジャンル16を横断取得・重複排除）
  → data/<area>.json をリポジトリにコミット
       ↓ 同一オリジンで配信（CORSなし・キーなし・高速）
アプリ: data/<area>.json を読み、既存の決定エンジンにそのまま流す
```

- 優先順位: プロキシ設定あり → 事前取得データ → APIキー直接 → デモデータ
- 現在地選択時は最寄りエリアのファイルを使い、徒歩分数は実際の緯度経度から算出（分速80m）
- 0件時は条件を緩めず「見つかりませんでした」画面で理由を明示し、エリア変更/条件変更へ誘導する

## 6. 技術ノート

- エンドポイント: `https://webservice.recruit.co.jp/hotpepper/gourmet/v1/`（無料・要APIキー登録）
- **CORS非対応**。選択肢：
  - MVP: `format=jsonp&callback=` で静的ページから直接（キー露出・レート制限は許容）
  - 公開時: 軽量プロキシでキー秘匿＋キャッシュ（本リポジトリはFirebase利用中 → Functions が親和。Cloudflare Workersでも可）
- レート制限あり（目安3,000req/日と言われる。登録時の案内で要確認）→ プロキシ側で「エリア×シーン」単位の短期キャッシュを入れると実質問題にならない
- **【実装初日タスク】本書のコード表（G001〜G017 / B001〜B014 / range / 各フラグ）はマスタAPI（`/genre/v1/`,`/budget/v1/`）と公式リファレンスで必ず突合する**（本書は学習知識ベースで作成のため）
- 実装形態: `gourmet/index.html` 単一ファイル（リポジトリの流儀に合わせる）。スマホ縦・ダークトーン基調

## 7. MVPの範囲

**やる**: 現在地（＋フォールバック1エリア）／4シーン×3問／言い切り結果＋違うのがいい／LINE共有URL／理由タグ
**やらない**: アカウント・ログイン／お気に入り保存／レビュー・投稿／ネイティブアプリ／複数都市展開／インスタAPI連携（インスタは手動運用の入口）

## 8. 検証計画（開発と並行）

1. インスタアカウント開設 → ストーリーズの4択スティッカーで質問フォーマットを手動運用（回答率＝需要の一次検証）
2. リール「◯曜◯時・◯◯・ふたり・しっぽり → ここ」を定型フォーマット化
3. プロフィールURL → 本アプリ。結果画面からの「LINEで送る」率を最重要指標に（共有される＝決定を任された証拠）
